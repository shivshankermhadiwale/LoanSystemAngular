import { LoanCollectionSummary } from './loan-collection-summary';

describe('LoanCollectionSummary', () => {
  it('should create an instance', () => {
    expect(new LoanCollectionSummary()).toBeTruthy();
  });
});
